# Absolute path recommended for multi-client setups
MCP_DB_PATH=

# hash (default, local) or openai
MCP_EMBEDDING_PROVIDER=hash
MCP_OPENAI_EMBEDDING_MODEL=text-embedding-3-small

MCP_DEFAULT_TOP_K=10
MCP_MAX_TOP_K=50
MCP_LOG_LEVEL=info

# Optional (required only for openai embeddings)
OPENAI_API_KEY=
